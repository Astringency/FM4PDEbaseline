name: recfno_variable_sensors_20260914
config: baselines/configs/paper.yaml
data_files_config: configs/data_files/formal_128.yaml
pdes: [poisson, helmholtz, darcy, nsnonbounded]
baselines: [recfno]
seeds: [1]
task: sparse_solution_multicondition
task_protocol_version: fm4pde-sparse-solution-multicondition-v1
sensor_mode: random_per_sample
sensor_budget_mode: total
num_sensors: 500
train_size: 50000
val_size: 5000
test_size: 1000
train_shards: 5
condition_mode: mixed
condition_probabilities: {a_only: 0.3333333333, u_only: 0.3333333333, both: 0.3333333334}
training_regimes:
  fixed500: [500]
  mixed: [50, 100, 250, 500, 1000]
evaluation_sensor_counts: [50, 100, 250, 500, 1000]
evaluation_condition_modes: [a_only, u_only, both]
evaluation_distributions: [original, id, smooth, rough]
primary_comparison_sensor_count: 500
batch_size: 16
epochs: 200
selection: original early stopping on validation at 500 points, all three conditions
